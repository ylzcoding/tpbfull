% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpb_full_pipeline.R
\name{tpb_full_pipeline}
\alias{tpb_full_pipeline}
\title{TPB Full Pipeline with Optional EB Prior Elicitation and Multi-Chain MCMC}
\usage{
tpb_full_pipeline(
  X,
  y,
  num_chains = 4,
  auto_find = TRUE,
  hyper_params = list(prior_type_a = "gamma", prior_type_b = "gamma", s_a = 1.5, r_a = 1,
    s_b = 1.5, r_b = 1, scale_a = 1, scale_b = 1, scale_phi = NULL),
  num_iter = 1e+05,
  num_warmup = 25000,
  thinning = 1,
  iter_pre_opt = 100,
  pre_opt_burnin = 200,
  pre_opt_samples = 200,
  omega_init_guess = NULL,
  sigmaSq_init_guess = NULL,
  init_method = c("ridge", "olasso"),
  ridge_lambda = NULL,
  selection_samples = 200,
  selection_score = c("posterior", "prior"),
  IS_period = 1,
  min_em_ess_fraction = 0.1,
  woodbury = TRUE,
  diagX = FALSE,
  candidates = tpb_default_candidates(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{n*p design matrix}

\item{y}{n*1 response vector}

\item{num_chains}{Integer, number of independent MCMC chains to run}

\item{auto_find}{Logical, run empirical Bayes model competition once before MCMC}

\item{hyper_params}{List of hyperparameters for the fully Bayesian sampler}

\item{num_iter}{Integer, total number of MCMC iterations across all chains}

\item{num_warmup}{Integer, total number of warm-up iterations across all chains}

\item{thinning}{Integer, thinning interval for saved posterior samples}

\item{iter_pre_opt}{Iterations for the model competition pre-optimization EM stage for each candidate}

\item{pre_opt_burnin, pre_opt_samples}{Burn-in and saved Gibbs samples within each EM E-step}

\item{omega_init_guess, sigmaSq_init_guess}{Optional initial guesses for EB omega and sigmaSq}

\item{init_method}{Method used to initialize EB omega and sigmaSq when
explicit initial guesses are not supplied. "ridge" uses ridge regression;
"olasso" uses organic lasso.}

\item{ridge_lambda}{Ridge penalty used when init_method = "ridge". If NULL,
sqrt(n) is used.}

\item{selection_samples}{Number of posterior samples per candidate used by
the reverse-logistic selection stage.}

\item{selection_score}{Score used in candidate selection. "prior" uses the
marginal TPB beta density; "posterior" additionally includes the Gaussian
likelihood with candidate sigmaSq.}

\item{IS_period}{Pre-optimization EM resampling period. 1 runs a fresh Gibbs
E-step every EM iteration; values above 1 reuse samples with importance
weights between resampling iterations.}

\item{min_em_ess_fraction}{Minimum effective sample size fraction required
to reuse pre-optimization EM samples with importance weights.}

\item{woodbury}{Logical, use Woodbury identity in both model competition and full Gibbs sampling}

\item{diagX}{Logical, assume diagonal X}

\item{candidates}{Candidate models used when auto_find = TRUE.
This should be a named list; each entry must contain numeric a and b.
The default is tpb_default_candidates().}

\item{verbose}{Logical, print model competition progress messages.}

\item{...}{Additional arguments passed to fullGibbs()}
}
\value{
A list containing combined samples, per-chain outputs, diagnostics,
  hyperparameters used, and optional EB competition metadata.
}
\description{
TPB Full Pipeline with Optional EB Prior Elicitation and Multi-Chain MCMC
}
