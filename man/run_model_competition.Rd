% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_competition.R
\name{run_model_competition}
\alias{run_model_competition}
\title{Run Empirical Bayes Model Competition for TPB Prior Elicitation}
\usage{
run_model_competition(
  X,
  y,
  iter_pre_opt = 100,
  omega_init_guess = NULL,
  sigmaSq_init_guess = NULL,
  init_method = c("ridge", "olasso"),
  ridge_lambda = NULL,
  iter_selection = 5000,
  woodbury = TRUE,
  candidates = tpb_default_candidates(),
  pre_opt_burnin = 200,
  pre_opt_samples = 200,
  iter_burnin_selection = 0,
  delta1 = 1e-06,
  delta2 = 0.001,
  delta3 = 0.001,
  window_size = 5,
  diagX = FALSE
)
}
\arguments{
\item{X}{n*p design matrix}

\item{y}{n*1 response vector}

\item{iter_pre_opt}{Iterations for the pre-optimization EM stage for each candidate.}

\item{omega_init_guess, sigmaSq_init_guess}{Optional initial guesses for omega and sigmaSq.
In the E-step, beta_j | nu_j, lambda_j uses variance omega * nu_j / lambda_j.}

\item{init_method}{Method used to initialize omega and sigmaSq when explicit
initial guesses are not supplied. "ridge" uses a ridge regression residual
variance; "olasso" uses organic lasso.}

\item{ridge_lambda}{Ridge penalty used when init_method = "ridge". If NULL,
sqrt(n) is used.}

\item{iter_selection}{Number of post-burn-in samples for model selection.}

\item{woodbury}{Logical, use Woodbury identity in beta updates.}

\item{candidates}{Candidate models and their initial a,b values.}

\item{pre_opt_burnin, pre_opt_samples}{Burn-in and saved Gibbs samples within each EM E-step.}

\item{iter_burnin_selection}{Burn-in iterations for the model indicator chain.}

\item{delta1, delta2, delta3}{Convergence tolerances.}

\item{window_size}{Size of the convergence window.}

\item{diagX}{Logical, assume diagonal X.}
}
\value{
A list with winner, winner_name, and raw adaptive competition output.
}
\description{
Run Empirical Bayes Model Competition for TPB Prior Elicitation
}
